troubleshoot the lint issue (check next CLI invocation and environment) and patch the script if needed.troubleshoot the lint issue (check next CLI invocation and environment) and patch the script if needed.# Art Frames E-Commerce Site

This repository contains the Next.js storefront app only.

## Getting Started

```bash
pnpm install
pnpm dev
```

## Notes

- Scrapers live in a separate repo (art-frames-scrapers).
- Supabase is used by the app only. Scraper integration is not set up yet.
